# ==============================================
# MSHKLTK ENVIRONMENT VARIABLES
# ==============================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control

# ==============================================
# CRITICAL SECURITY SETTINGS
# ==============================================

# JWT Secret for authentication (REQUIRED)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-now

# Allowed CORS origins (comma-separated)
# Production example: https://mshkltk.com,https://www.mshkltk.com
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ==============================================
# DATABASE CONFIGURATION
# ==============================================

# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mshkltk
DB_USER=postgres
DB_PASSWORD=your-postgres-password

# Database connection pool settings
DB_POOL_MAX=20
DB_POOL_MIN=5
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=2000

# For Google Cloud SQL deployment (optional)
# CLOUD_SQL_CONNECTION_NAME=project:region:instance

# ==============================================
# AI SERVICES
# ==============================================

# Google Gemini API Key (REQUIRED for AI features)
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# ==============================================
# GOOGLE CLOUD STORAGE (OPTIONAL)
# ==============================================

# For file uploads to GCS
# GOOGLE_CLOUD_PROJECT_ID=your-project-id
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
# GOOGLE_CLOUD_STORAGE_BUCKET=mshkltk-media

# ==============================================
# APPLICATION SETTINGS
# ==============================================

# Node environment
NODE_ENV=development

# Server port
PORT=3001

# Frontend URL (for CORS and redirects)
VITE_API_BASE_URL=http://localhost:3001/api

# Enable debug logging for database queries
# DEBUG_QUERIES=true

# ==============================================
# SECURITY NOTES
# ==============================================
# 
# 1. JWT_SECRET: Use a strong random string (32+ characters)
#    Never use default values in production
#
# 2. ALLOWED_ORIGINS: Restrict to your actual domains
#    Remove localhost in production
#
# 3. DB_PASSWORD: Use a strong password
#    Store securely in production (e.g., Secret Manager)
#
# 4. GEMINI_API_KEY: Protect this key carefully
#    Monitor usage to prevent quota exhaustion
#
# 5. All secrets: Consider using a secrets manager
#    in production (GCP Secret Manager, AWS Secrets Manager, etc.)
