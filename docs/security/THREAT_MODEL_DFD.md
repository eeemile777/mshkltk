# Data Flow Diagram (DFD) - Mshkltk Threat Modeling

**Purpose:** Visual representation of data flows for security threat analysis  
**Date:** November 16, 2025  
**Scope:** Complete system architecture including all three portals

---

## Table of Contents

1. [Context Diagram (Level 0)](#context-diagram-level-0)
2. [System Overview (Level 1)](#system-overview-level-1)
3. [Detailed Process DFD (Level 2)](#detailed-process-dfd-level-2)
4. [Trust Boundaries](#trust-boundaries)
5. [Data Stores](#data-stores)
6. [External Entities](#external-entities)
7. [Critical Data Flows](#critical-data-flows)

---

## Context Diagram (Level 0)

**High-level system boundaries and external actors**

```
┌─────────────────────────────────────────────────────────────────┐
│                         TRUST BOUNDARY                          │
│                                                                 │
│  ┌──────────┐         ┌─────────────────┐         ┌──────────┐ │
│  │ Citizens │◄───────►│                 │◄───────►│Municipal │ │
│  │ (Mobile/ │         │   Mshkltk PWA   │         │  Staff   │ │
│  │  Web)    │         │                 │         │(Portal)  │ │
│  └──────────┘         │  ┌───────────┐  │         └──────────┘ │
│                       │  │           │  │                       │
│  ┌──────────┐         │  │  Express  │  │         ┌──────────┐ │
│  │  Google  │◄───────►│  │  Backend  │  │◄───────►│  Super   │ │
│  │  Gemini  │         │  │           │  │         │  Admin   │ │
│  │   API    │         │  └─────┬─────┘  │         │          │ │
│  └──────────┘         │        │        │         └──────────┘ │
│                       │        ▼        │                       │
│                       │  ┌───────────┐  │                       │
│                       │  │PostgreSQL │  │                       │
│                       │  │ + PostGIS │  │                       │
│                       │  └───────────┘  │                       │
│                       └─────────────────┘                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

External Entities (Outside Trust Boundary):
├── Citizens (Untrusted - Anonymous/Registered)
├── Municipal Staff (Semi-Trusted - Authenticated)
├── Super Admin (Trusted - Authenticated)
└── Google Gemini AI (External Service - API Key Auth)
```

**Trust Levels:**
- 🔴 **Untrusted**: Anonymous citizens (public internet)
- 🟡 **Semi-Trusted**: Authenticated users (citizens, municipal staff)
- 🟢 **Trusted**: Super admins, internal services
- 🔵 **External**: Third-party services (Google Gemini)

---

## System Overview (Level 1)

**Main processes and data flows**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              MSHKLTK SYSTEM                                 │
│                                                                             │
│  ┌──────────┐                                                               │
│  │ Citizens │                                                               │
│  └────┬─────┘                                                               │
│       │                                                                     │
│       │ (1) Registration/Login [Username, Password]                        │
│       │                                                                     │
│       ▼                                                                     │
│  ┌─────────────────────────────────────────────┐                           │
│  │  1.0 Authentication & Authorization         │                           │
│  │  ┌────────────────────────────────────┐     │                           │
│  │  │ • JWT Token Generation/Validation  │     │                           │
│  │  │ • bcrypt Password Hashing          │     │                           │
│  │  │ • Role-Based Access Control        │     │                           │
│  │  │ • Rate Limiting (5 req/15min)      │     │                           │
│  │  └────────────────────────────────────┘     │                           │
│  └──────────────────┬──────────────────────────┘                           │
│                     │                                                       │
│                     │ (2) JWT Token [Authorization: Bearer <token>]        │
│                     │                                                       │
│                     ▼                                                       │
│  ┌──────────────────────────────────────────────┐                          │
│  │  2.0 Report Management                       │                          │
│  │  ┌─────────────────────────────────────┐     │                          │
│  │  │ • Create Report (+ Photos/Videos)   │     │                          │
│  │  │ • Update Status (Portal/Admin only) │     │                          │
│  │  │ • Confirm Report (Citizens)         │     │                          │
│  │  │ • Subscribe to Updates              │     │                          │
│  │  │ • Filter by Municipality/Category   │     │                          │
│  │  └─────────────────────────────────────┘     │                          │
│  └──────────────────┬───────────────────────────┘                          │
│                     │                                                       │
│                     │ (3) Report Data [Title, Location, Photos, Category]  │
│                     │                                                       │
│                     ▼                                                       │
│  ┌──────────────────────────────────────────────┐                          │
│  │  3.0 AI Analysis (Google Gemini)             │                          │
│  │  ┌─────────────────────────────────────┐     │                          │
│  │  │ • Analyze Photos (categorization)   │     │                          │
│  │  │ • Detect Municipality (geolocation) │     │                          │
│  │  │ • Generate Title (description)      │     │                          │
│  │  │ • Transcribe Audio (speech-to-text) │     │                          │
│  │  └─────────────────────────────────────┘     │                          │
│  └──────────────────┬───────────────────────────┘                          │
│                     │                                                       │
│                     │ (4) AI Results [Category, Municipality, Title]       │
│                     │                                                       │
│                     ▼                                                       │
│  ┌──────────────────────────────────────────────┐                          │
│  │  4.0 Gamification & Notifications            │                          │
│  │  ┌─────────────────────────────────────┐     │                          │
│  │  │ • Award Points (Report +10, etc.)   │     │                          │
│  │  │ • Badge Achievement Detection       │     │                          │
│  │  │ • Notification Creation/Dispatch    │     │                          │
│  │  │ • Level Calculation                 │     │                          │
│  │  └─────────────────────────────────────┘     │                          │
│  └──────────────────┬───────────────────────────┘                          │
│                     │                                                       │
│                     │ (5) User Updates [Points, Badges, Notifications]     │
│                     │                                                       │
│                     ▼                                                       │
│  ┌──────────────────────────────────────────────┐                          │
│  │  5.0 Data Persistence (PostgreSQL)           │                          │
│  │  ┌─────────────────────────────────────┐     │                          │
│  │  │ • Users (passwords hashed + salted) │     │                          │
│  │  │ • Reports (with PostGIS location)   │     │                          │
│  │  │ • Comments                          │     │                          │
│  │  │ • Notifications                     │     │                          │
│  │  │ • Report History (audit trail)      │     │                          │
│  │  │ • Audit Logs (super admin actions) │     │                          │
│  │  └─────────────────────────────────────┘     │                          │
│  └──────────────────────────────────────────────┘                          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                           PARALLEL PROCESSES                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌──────────────────────────────────────┐   ┌───────────────────────────┐  │
│  │  6.0 Municipal Portal                │   │  7.0 Super Admin Portal   │  │
│  │  • View Assigned Reports             │   │  • User Management        │  │
│  │  • Update Report Status              │   │  • Category Management    │  │
│  │  • Add Comments                      │   │  • Badge Configuration    │  │
│  │  • Filter by Scope (municipality)    │   │  • Audit Log Review       │  │
│  └──────────────────────────────────────┘   │  • Portal Impersonation   │  │
│                                             └───────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Detailed Process DFD (Level 2)

### 2.1 Authentication Process

```
                    ┌──────────────────────────────────────────┐
                    │         AUTHENTICATION FLOW              │
                    └──────────────────────────────────────────┘

┌──────────┐                                              ┌──────────────┐
│ Citizen/ │                                              │   Database   │
│  Staff/  │                                              │ (Users Table)│
│  Admin   │                                              └──────────────┘
└────┬─────┘                                                      ▲
     │                                                            │
     │ (A) POST /api/auth/register                               │
     │     [username, password, display_name, role]              │
     │                                                            │
     ▼                                                            │
┌─────────────────────────────────────────────┐                  │
│  Rate Limiter (5 req/15min per IP)          │                  │
│  ┌────────────────────────────────────┐     │                  │
│  │ Check IP address against cache     │     │                  │
│  │ Reject if limit exceeded           │     │                  │
│  └────────────────────────────────────┘     │                  │
└──────────────────┬──────────────────────────┘                  │
                   │                                              │
                   │ (B) Validated Request                       │
                   │                                              │
                   ▼                                              │
┌─────────────────────────────────────────────┐                  │
│  Input Validation (express-validator)       │                  │
│  ┌────────────────────────────────────┐     │                  │
│  │ • Username: 3-50 chars, alphanumeric│     │                  │
│  │ • Password: 8+ chars, complexity   │     │                  │
│  │ • Display Name: 1-255 chars        │     │                  │
│  │ • Role: enum validation            │     │                  │
│  └────────────────────────────────────┘     │                  │
└──────────────────┬──────────────────────────┘                  │
                   │                                              │
                   │ (C) Valid Input                             │
                   │                                              │
                   ▼                                              │
┌─────────────────────────────────────────────┐                  │
│  Password Hashing (bcrypt)                  │                  │
│  ┌────────────────────────────────────┐     │                  │
│  │ 1. Generate random salt (10 rounds)│     │                  │
│  │ 2. Hash password + salt            │     │                  │
│  │ 3. Store hash and salt separately  │     │                  │
│  └────────────────────────────────────┘     │                  │
└──────────────────┬──────────────────────────┘                  │
                   │                                              │
                   │ (D) [password_hash, salt]                   │
                   │                                              │
                   ▼                                              │
┌─────────────────────────────────────────────┐                  │
│  User Creation                              │                  │
│  ┌────────────────────────────────────┐     │                  │
│  │ INSERT INTO users VALUES (...)     │     │──────────────────┘
│  │ - id: UUID (auto-generated)        │     │
│  │ - username: unique constraint      │     │
│  │ - password_hash: hashed            │     │
│  │ - salt: random                     │     │
│  │ - role: citizen/portal/super_admin │     │
│  │ - points: 0 (default)              │     │
│  └────────────────────────────────────┘     │
└──────────────────┬──────────────────────────┘
                   │
                   │ (E) User Created
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  JWT Token Generation                       │
│  ┌────────────────────────────────────┐     │
│  │ Payload: {                         │     │
│  │   id: user.id,                     │     │
│  │   username: user.username,         │     │
│  │   role: user.role,                 │     │
│  │   municipality_id: user.muni_id,   │     │
│  │   portal_access_level: user.access │     │
│  │ }                                  │     │
│  │ Secret: JWT_SECRET (env var)       │     │
│  │ Expiry: 7 days                     │     │
│  └────────────────────────────────────┘     │
└──────────────────┬──────────────────────────┘
                   │
                   │ (F) JWT Token
                   │
                   ▼
              ┌─────────┐
              │ Response│
              │ { token }│
              └─────────┘

LOGIN FLOW (Similar):
POST /api/auth/login → Rate Limiter → Validate Input → 
Check Username Exists → Verify Password (bcrypt.compare) → 
Generate JWT → Return Token

TOKEN VERIFICATION (Middleware):
Authorization Header → Extract Token → Verify Signature → 
Check Expiry → Decode Payload → Attach user to req.user
```

### 2.2 Report Submission Process

```
                    ┌──────────────────────────────────────────┐
                    │        REPORT SUBMISSION FLOW            │
                    └──────────────────────────────────────────┘

┌──────────┐                                              ┌──────────────┐
│ Citizen  │                                              │   Database   │
└────┬─────┘                                              │ (Reports)    │
     │                                                    └──────────────┘
     │ (1) POST /api/reports                                     ▲
     │     Authorization: Bearer <JWT>                           │
     │     Body: {                                               │
     │       title_en, title_ar,                                 │
     │       note_en, note_ar,                                   │
     │       lat, lng, municipality,                             │
     │       category, photos (base64[])                         │
     │     }                                                     │
     │                                                           │
     ▼                                                           │
┌─────────────────────────────────────────────┐                 │
│  Auth Middleware (JWT Verification)         │                 │
│  ┌────────────────────────────────────┐     │                 │
│  │ • Extract token from header        │     │                 │
│  │ • Verify signature with JWT_SECRET │     │                 │
│  │ • Check expiration (7 days)        │     │                 │
│  │ • Decode payload → req.user        │     │                 │
│  └────────────────────────────────────┘     │                 │
└──────────────────┬──────────────────────────┘                 │
                   │                                             │
                   │ (2) Authenticated User                      │
                   │                                             │
                   ▼                                             │
┌─────────────────────────────────────────────┐                 │
│  Input Validation                           │                 │
│  ┌────────────────────────────────────┐     │                 │
│  │ • Titles: 1-500 chars each         │     │                 │
│  │ • Notes: 1-5000 chars each         │     │                 │
│  │ • Lat/Lng: Valid coordinates       │     │                 │
│  │ • Municipality: non-empty string   │     │                 │
│  │ • Category: valid enum             │     │                 │
│  │ • Photos: array, max 10 items      │     │                 │
│  └────────────────────────────────────┘     │                 │
└──────────────────┬──────────────────────────┘                 │
                   │                                             │
                   │ (3) Valid Input                            │
                   │                                             │
                   ▼                                             │
┌─────────────────────────────────────────────┐                 │
│  Photo Processing (if photos provided)      │                 │
│  ┌────────────────────────────────────┐     │                 │
│  │ FOR EACH photo (base64 string):    │     │                 │
│  │   1. Decode base64                 │     │                 │
│  │   2. Validate image format         │     │                 │
│  │   3. Save to /uploads/<uuid>.jpg   │     │                 │
│  │   4. Store URL in photo_urls[]     │     │                 │
│  └────────────────────────────────────┘     │                 │
└──────────────────┬──────────────────────────┘                 │
                   │                                             │
                   │ (4) Photo URLs                             │
                   │                                             │
                   ▼                                             │
┌─────────────────────────────────────────────┐                 │
│  AI Analysis (Google Gemini)                │                 │
│  ┌────────────────────────────────────┐     │                 │
│  │ POST /api/ai/analyze-media          │     │                 │
│  │ Send:                               │     │                 │
│  │   - Photo (base64)                  │     │                 │
│  │   - Description (note_en)           │     │                 │
│  │   - Location (lat, lng)             │     │                 │
│  │ Receive:                            │     │                 │
│  │   - Detected category               │     │                 │
│  │   - Severity level                  │     │                 │
│  │   - Auto-generated title (optional) │     │                 │
│  └────────────────────────────────────┘     │                 │
└──────────────────┬──────────────────────────┘                 │
                   │                                             │
                   │ (5) AI Results                             │
                   │     [category, severity]                   │
                   │                                             │
                   ▼                                             │
┌─────────────────────────────────────────────┐                 │
│  Create Report Record                       │                 │
│  ┌────────────────────────────────────┐     │                 │
│  │ INSERT INTO reports:               │     │─────────────────┘
│  │   id = uuid_generate_v4()          │     │
│  │   title_en, title_ar               │     │
│  │   photo_urls = [url1, url2, ...]   │     │
│  │   location = ST_SetSRID(           │     │ (PostGIS)
│  │     ST_MakePoint(lng, lat), 4326)  │     │
│  │   lat, lng                         │     │
│  │   municipality, category           │     │
│  │   note_en, note_ar                 │     │
│  │   status = 'new'                   │     │
│  │   severity = AI result or 'medium' │     │
│  │   created_by = req.user.id         │     │
│  │   created_at = NOW()               │     │
│  └────────────────────────────────────┘     │
└──────────────────┬──────────────────────────┘
                   │
                   │ (6) Report Created
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  Gamification (Award Points)                │
│  ┌────────────────────────────────────┐     │
│  │ UPDATE users                       │     │
│  │ SET points = points + 10,          │     │
│  │     reports_count = reports_count+1│     │
│  │ WHERE id = created_by              │     │
│  │                                    │     │
│  │ Check for badge achievements:      │     │
│  │  - First Report → 'First Reporter' │     │
│  │  - 5 Reports → 'Active Citizen'    │     │
│  └────────────────────────────────────┘     │
└──────────────────┬──────────────────────────┘
                   │
                   │ (7) Points Awarded
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  Notification Creation                      │
│  ┌────────────────────────────────────┐     │
│  │ IF badge earned:                   │     │
│  │   INSERT INTO notifications        │     │
│  │   (user_id, type='badge_earned',   │     │
│  │    message, report_id)             │     │
│  └────────────────────────────────────┘     │
└──────────────────┬──────────────────────────┘
                   │
                   │ (8) Notification Created
                   │
                   ▼
              ┌─────────┐
              │ Response│
              │ 201 Created                 │
              │ { report: {...}, points: 10 }│
              └─────────┘
```

### 2.3 Municipality Portal - Status Update

```
                    ┌──────────────────────────────────────────┐
                    │       PORTAL STATUS UPDATE FLOW          │
                    └──────────────────────────────────────────┘

┌──────────────┐                                        ┌──────────────┐
│ Portal Staff │                                        │   Database   │
└──────┬───────┘                                        └──────────────┘
       │                                                        ▲
       │ (1) PATCH /api/reports/:id                            │
       │     Authorization: Bearer <JWT>                       │
       │     Body: { status: 'in_progress' }                   │
       │                                                        │
       ▼                                                        │
┌─────────────────────────────────────────────┐                │
│  Auth Middleware                            │                │
│  ┌────────────────────────────────────┐     │                │
│  │ Verify JWT token                   │     │                │
│  │ Extract user role → req.user       │     │                │
│  └────────────────────────────────────┘     │                │
└──────────────────┬──────────────────────────┘                │
                   │                                            │
                   │ (2) Authenticated User                     │
                   │                                            │
                   ▼                                            │
┌─────────────────────────────────────────────┐                │
│  Role-Based Access Control                  │                │
│  ┌────────────────────────────────────┐     │                │
│  │ IF user.role === 'citizen':        │     │                │
│  │   → REJECT (403 Forbidden)         │     │                │
│  │ ELSE IF user.role === 'portal':    │     │                │
│  │   → Check scoped_municipalities    │     │                │
│  │   → Verify report.municipality     │     │                │
│  │      in user.scoped_municipalities │     │                │
│  │ ELSE IF user.role === 'super_admin'│     │                │
│  │   → ALLOW (full access)            │     │                │
│  └────────────────────────────────────┘     │                │
└──────────────────┬──────────────────────────┘                │
                   │                                            │
                   │ (3) Authorized                             │
                   │                                            │
                   ▼                                            │
┌─────────────────────────────────────────────┐                │
│  Fetch Existing Report                      │                │
│  ┌────────────────────────────────────┐     │                │
│  │ SELECT * FROM reports              │     │◄───────────────┘
│  │ WHERE id = :id                     │     │
│  └────────────────────────────────────┘     │
└──────────────────┬──────────────────────────┘
                   │
                   │ (4) Report Data
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  Validate Status Transition                 │
│  ┌────────────────────────────────────┐     │
│  │ Valid transitions:                 │     │
│  │  new → received                    │     │
│  │  new → in_progress                 │     │
│  │  received → in_progress            │     │
│  │  in_progress → resolved            │     │
│  │  * → new (super_admin only)        │     │
│  └────────────────────────────────────┘     │
└──────────────────┬──────────────────────────┘
                   │
                   │ (5) Valid Transition
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  Update Report (Transaction)                │
│  ┌────────────────────────────────────┐     │
│  │ BEGIN TRANSACTION;                 │     │
│  │                                    │     │
│  │ UPDATE reports                     │     │
│  │ SET status = :new_status           │     │
│  │ WHERE id = :id;                    │     │
│  │                                    │     │
│  │ INSERT INTO report_history         │     │
│  │ (report_id, status, changed_by,    │     │
│  │  changed_at)                       │     │
│  │ VALUES (:id, :new_status,          │     │
│  │         :user_id, NOW());          │     │
│  │                                    │     │
│  │ COMMIT;                            │     │
│  └────────────────────────────────────┘     │
└──────────────────┬──────────────────────────┘
                   │
                   │ (6) Report Updated
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  Notify Subscribers                         │
│  ┌────────────────────────────────────┐     │
│  │ FOR EACH user_id in                │     │
│  │   report.subscribed_user_ids:      │     │
│  │                                    │     │
│  │   INSERT INTO notifications        │     │
│  │   (user_id, report_id,             │     │
│  │    type='status_change',           │     │
│  │    message='Report status updated',│     │
│  │    is_read=false)                  │     │
│  └────────────────────────────────────┘     │
└──────────────────┬──────────────────────────┘
                   │
                   │ (7) Notifications Sent
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  Audit Logging                              │
│  ┌────────────────────────────────────┐     │
│  │ INSERT INTO audit_logs             │     │
│  │ (user_id, action, entity_type,     │     │
│  │  entity_id, old_value, new_value,  │     │
│  │  timestamp)                        │     │
│  │ VALUES (req.user.id,               │     │
│  │  'UPDATE_REPORT_STATUS', 'report', │     │
│  │  :id, :old_status, :new_status,    │     │
│  │  NOW())                            │     │
│  └────────────────────────────────────┘     │
└──────────────────┬──────────────────────────┘
                   │
                   │ (8) Audit Log Created
                   │
                   ▼
              ┌─────────┐
              │ Response│
              │ 200 OK                          │
              │ { report: {...} }               │
              └─────────┘
```

---

## Trust Boundaries

**Visual representation of security zones**

```
┌───────────────────────────────────────────────────────────────────┐
│                         INTERNET (Untrusted)                      │
│                                                                   │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐                   │
│  │Anonymous │    │Registered│    │Malicious │                   │
│  │Citizens  │    │Citizens  │    │Actors    │                   │
│  └──────────┘    └──────────┘    └──────────┘                   │
│         │              │                │                        │
└─────────┼──────────────┼────────────────┼────────────────────────┘
          │              │                │
          ▼              ▼                ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BOUNDARY 1: TLS/HTTPS                        │
│              (Transport Layer Encryption - Port 443)            │
└─────────────────────────────────────────────────────────────────┘
          │              │                │
          ▼              ▼                ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BOUNDARY 2: API Gateway                      │
│         (Rate Limiting, CORS, Helmet Security Headers)          │
│                                                                 │
│  ┌──────────────────────────────────────────────────────┐      │
│  │ Security Controls:                                   │      │
│  │ • Rate Limiter (5 req/15min on auth endpoints)       │      │
│  │ • CORS Whitelist (ALLOWED_ORIGINS env var)          │      │
│  │ • Helmet Headers (XSS, Clickjacking protection)      │      │
│  │ • Request Size Limits (1MB default, 50MB media)      │      │
│  └──────────────────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────────────────┘
          │              │
          ▼              ▼
┌─────────────────────────────────────────────────────────────────┐
│                  BOUNDARY 3: Authentication                     │
│                   (JWT Token Verification)                      │
│                                                                 │
│  ┌──────────────────────────────────────────────────────┐      │
│  │ Auth Middleware:                                     │      │
│  │ • Extract JWT from Authorization header              │      │
│  │ • Verify signature with JWT_SECRET                   │      │
│  │ • Check token expiration (7 days max)                │      │
│  │ • Decode payload → req.user                          │      │
│  └──────────────────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│                  BOUNDARY 4: Authorization                      │
│              (Role-Based Access Control - RBAC)                 │
│                                                                 │
│  ┌────────────────────┐  ┌────────────────────┐               │
│  │  Citizen Zone      │  │  Portal Staff Zone │               │
│  │                    │  │                    │               │
│  │ • Submit Reports   │  │ • View Reports     │               │
│  │ • Confirm Reports  │  │ • Update Status    │               │
│  │ • Add Comments     │  │ • Assign Reports   │               │
│  │ • View Own Data    │  │ • Scoped Access    │               │
│  └────────────────────┘  │   (municipality)   │               │
│                          └────────────────────┘               │
│                                                                │
│  ┌──────────────────────────────────────────────┐             │
│  │         Super Admin Zone (Highest Trust)     │             │
│  │                                              │             │
│  │ • Full Read/Write Access                     │             │
│  │ • User Management (CRUD)                     │             │
│  │ • Category/Badge Configuration               │             │
│  │ • Audit Log Access                           │             │
│  │ • Portal Impersonation                       │             │
│  └──────────────────────────────────────────────┘             │
└─────────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│                  BOUNDARY 5: Data Layer                         │
│                    (Database Access)                            │
│                                                                 │
│  ┌──────────────────────────────────────────────────────┐      │
│  │ PostgreSQL + PostGIS                                 │      │
│  │                                                      │      │
│  │ Security:                                            │      │
│  │ • Parameterized queries (SQL injection prevention)   │      │
│  │ • Connection pooling (max 20 connections)            │      │
│  │ • Database user with limited privileges              │      │
│  │ • Password hashing (bcrypt + salt)                   │      │
│  │ • Sensitive data at rest (encrypted volume - prod)   │      │
│  └──────────────────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│              EXTERNAL BOUNDARY: Third-Party APIs                │
│                                                                 │
│  ┌──────────────────────────────────────────────────────┐      │
│  │ Google Gemini API                                    │      │
│  │                                                      │      │
│  │ • API Key Authentication (GEMINI_API_KEY)            │      │
│  │ • Rate limiting (external service)                   │      │
│  │ • Data sanitization before sending                   │      │
│  │ • No PII sent to external service                    │      │
│  └──────────────────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Stores

**Persistent storage and sensitivity levels**

```
┌─────────────────────────────────────────────────────────────────┐
│                   DATA STORES (PostgreSQL 15)                   │
└─────────────────────────────────────────────────────────────────┘

DS1: Users Table
┌──────────────────────────────────────────────────────────┐
│ Sensitivity: 🔴 CRITICAL (PII + Credentials)             │
├──────────────────────────────────────────────────────────┤
│ Fields:                                                  │
│  • id (UUID, primary key)                                │
│  • username (unique, indexed)                            │
│  • password_hash (bcrypt hashed) ⚠️ SENSITIVE            │
│  • salt (random, per-user) ⚠️ SENSITIVE                  │
│  • first_name, last_name, display_name                   │
│  • points, achievements, reports_count                   │
│  • role (citizen/portal/super_admin)                     │
│  • municipality_id (for portal users)                    │
│  • scoped_categories, scoped_municipalities              │
│  • is_active, created_at                                 │
├──────────────────────────────────────────────────────────┤
│ Threats:                                                 │
│  • Credential theft (password hashes)                    │
│  • Privilege escalation (role manipulation)              │
│  • PII exposure (names, usernames)                       │
│  • Account enumeration via username                      │
├──────────────────────────────────────────────────────────┤
│ Mitigations:                                             │
│  ✅ bcrypt + salt (slow hashing, 10 rounds)              │
│  ✅ Unique constraints on username                       │
│  ✅ Role-based access control                            │
│  ✅ Parameterized queries (SQL injection prevention)     │
│  ✅ No passwords logged (sanitized)                      │
└──────────────────────────────────────────────────────────┘

DS2: Reports Table
┌──────────────────────────────────────────────────────────┐
│ Sensitivity: 🟡 MODERATE (Location Data + User Content) │
├──────────────────────────────────────────────────────────┤
│ Fields:                                                  │
│  • id (UUID, primary key)                                │
│  • title_en, title_ar                                    │
│  • photo_urls[] (array of URLs)                          │
│  • location (PostGIS GEOGRAPHY) ⚠️ LOCATION DATA         │
│  • lat, lng (decimal coordinates)                        │
│  • municipality, area                                    │
│  • category, sub_category                                │
│  • note_en, note_ar (user description)                   │
│  • status, severity                                      │
│  • confirmations_count                                   │
│  • created_by (FK to users.id)                           │
│  • subscribed_user_ids[] (array)                         │
│  • created_at                                            │
├──────────────────────────────────────────────────────────┤
│ Threats:                                                 │
│  • User tracking via location data                       │
│  • Injection attacks (XSS in titles/notes)               │
│  • Unauthorized photo access                             │
│  • Data scraping (bulk export)                           │
├──────────────────────────────────────────────────────────┤
│ Mitigations:                                             │
│  ✅ Input validation (max lengths, allowed chars)        │
│  ✅ Output encoding (prevent XSS)                        │
│  ✅ Auth required for photo uploads                      │
│  ✅ Rate limiting on report creation                     │
│  ✅ PostGIS spatial indexing (performance)               │
└──────────────────────────────────────────────────────────┘

DS3: Comments Table
┌──────────────────────────────────────────────────────────┐
│ Sensitivity: 🟢 LOW (Public User Content)               │
├──────────────────────────────────────────────────────────┤
│ Fields:                                                  │
│  • id (UUID, primary key)                                │
│  • report_id (FK to reports.id, cascades on delete)      │
│  • user_id (FK to users.id, set null on delete)          │
│  • text (user comment)                                   │
│  • created_at                                            │
├──────────────────────────────────────────────────────────┤
│ Threats:                                                 │
│  • Spam/abuse (malicious comments)                       │
│  • XSS injection in comment text                         │
├──────────────────────────────────────────────────────────┤
│ Mitigations:                                             │
│  ✅ Input validation (1-2000 chars, sanitized)           │
│  ✅ Auth required to post comments                       │
│  ✅ Cascade delete (report deleted → comments deleted)   │
└──────────────────────────────────────────────────────────┘

DS4: Notifications Table
┌──────────────────────────────────────────────────────────┐
│ Sensitivity: 🟢 LOW (User Notifications)                │
├──────────────────────────────────────────────────────────┤
│ Fields:                                                  │
│  • id (UUID, primary key)                                │
│  • user_id (FK to users.id)                              │
│  • report_id (FK to reports.id, nullable)                │
│  • type (enum: status_change, new_comment, etc.)         │
│  • message (notification text)                           │
│  • is_read (boolean)                                     │
│  • created_at                                            │
├──────────────────────────────────────────────────────────┤
│ Threats:                                                 │
│  • Notification flooding (DoS)                           │
│  • Information disclosure (read other users' notifs)     │
├──────────────────────────────────────────────────────────┤
│ Mitigations:                                             │
│  ✅ Auth check (user can only see own notifications)     │
│  ✅ Rate limiting on notification creation               │
└──────────────────────────────────────────────────────────┘

DS5: Audit Logs Table
┌──────────────────────────────────────────────────────────┐
│ Sensitivity: 🔴 CRITICAL (Security Audit Trail)         │
├──────────────────────────────────────────────────────────┤
│ Fields:                                                  │
│  • id (UUID, primary key)                                │
│  • user_id (FK to users.id)                              │
│  • action (string: UPDATE_REPORT_STATUS, etc.)           │
│  • entity_type (report/user/config)                      │
│  • entity_id (UUID of affected entity)                   │
│  • old_value, new_value (JSON)                           │
│  • timestamp                                             │
│  • ip_address (client IP)                                │
├──────────────────────────────────────────────────────────┤
│ Threats:                                                 │
│  • Audit log tampering (cover tracks)                    │
│  • Unauthorized access to audit logs                     │
├──────────────────────────────────────────────────────────┤
│ Mitigations:                                             │
│  ✅ Super admin only access                              │
│  ✅ Append-only (no UPDATE/DELETE on logs)               │
│  ✅ Sensitive data sanitized before logging              │
└──────────────────────────────────────────────────────────┘

DS6: Report History Table
┌──────────────────────────────────────────────────────────┐
│ Sensitivity: 🟡 MODERATE (Historical Tracking)          │
├──────────────────────────────────────────────────────────┤
│ Fields:                                                  │
│  • id (UUID, primary key)                                │
│  • report_id (FK to reports.id, cascades on delete)      │
│  • status (report_status enum)                           │
│  • changed_by (FK to users.id)                           │
│  • changed_at (timestamp)                                │
├──────────────────────────────────────────────────────────┤
│ Threats:                                                 │
│  • History tampering (rewrite timeline)                  │
├──────────────────────────────────────────────────────────┤
│ Mitigations:                                             │
│  ✅ Append-only (INSERT only, no UPDATE)                 │
│  ✅ Cascade delete with parent report                    │
└──────────────────────────────────────────────────────────┘

DS7: File System (Photo/Video Storage)
┌──────────────────────────────────────────────────────────┐
│ Sensitivity: 🟡 MODERATE (User-Uploaded Media)          │
├──────────────────────────────────────────────────────────┤
│ Storage:                                                 │
│  • Local: /uploads/<uuid>.<ext>                          │
│  • Future: Google Cloud Storage buckets                  │
├──────────────────────────────────────────────────────────┤
│ Threats:                                                 │
│  • Malicious file uploads (malware, scripts)             │
│  • Path traversal attacks (../../etc/passwd)             │
│  • Unauthorized file access                              │
│  • Storage exhaustion (upload flooding)                  │
├──────────────────────────────────────────────────────────┤
│ Mitigations:                                             │
│  ✅ File type validation (image/video only)              │
│  ✅ File size limits (50MB max)                          │
│  ✅ UUID-based filenames (no user input in paths)        │
│  ✅ Auth required for uploads                            │
│  ⚠️  TODO: Virus scanning (production)                   │
│  ⚠️  TODO: CDN + signed URLs (production)                │
└──────────────────────────────────────────────────────────┘
```

---

## External Entities

**Actors and external systems**

```
┌─────────────────────────────────────────────────────────────────┐
│                      EXTERNAL ENTITIES                          │
└─────────────────────────────────────────────────────────────────┘

EE1: Anonymous Citizens
├─ Trust Level: 🔴 Untrusted
├─ Access: Public endpoints (read-only)
├─ Actions:
│  • Browse reports (map view)
│  • View report details
│  • Register for account
├─ Threats:
│  • Bot registration
│  • Data scraping
│  • Account enumeration
└─ Mitigations:
   ✅ Rate limiting
   ✅ CAPTCHA (future enhancement)
   ✅ Email verification (future)

EE2: Registered Citizens
├─ Trust Level: 🟡 Semi-Trusted (Authenticated)
├─ Access: Authenticated endpoints
├─ Actions:
│  • Submit reports
│  • Confirm other reports
│  • Add comments
│  • Subscribe to reports
│  • View gamification stats
├─ Threats:
│  • Fake/spam reports
│  • Report bombing (flooding)
│  • Malicious photo uploads
│  • XSS injection attempts
└─ Mitigations:
   ✅ JWT authentication
   ✅ Input validation
   ✅ Rate limiting
   ✅ File type validation
   ✅ Gamification (reward good behavior)

EE3: Municipal Portal Staff
├─ Trust Level: 🟡 Semi-Trusted (Authenticated + Scoped)
├─ Access: Portal-specific endpoints
├─ Actions:
│  • View assigned reports (scoped by municipality)
│  • Update report status
│  • Assign reports to staff
│  • Add internal comments
├─ Threats:
│  • Unauthorized access to other municipalities
│  • Status manipulation (fake resolutions)
│  • Privilege escalation attempts
└─ Mitigations:
   ✅ JWT authentication
   ✅ Scoped access (municipality filtering)
   ✅ RBAC (portal_access_level)
   ✅ Audit logging (all actions tracked)
   ✅ Status transition validation

EE4: Super Administrators
├─ Trust Level: 🟢 Trusted (Full Privileges)
├─ Access: All endpoints + admin-only routes
├─ Actions:
│  • User management (CRUD)
│  • Category/badge configuration
│  • Audit log review
│  • Portal impersonation
│  • System configuration
├─ Threats:
│  • Compromised admin account (highest impact)
│  • Accidental data deletion
│  • Insider threats
└─ Mitigations:
   ✅ Strong password requirements
   ✅ JWT authentication
   ✅ Audit logging (all actions tracked)
   ✅ Multi-factor authentication (future)
   ✅ Session timeout (7 days max)

EE5: Google Gemini AI Service
├─ Trust Level: 🔵 External Service (API Key Auth)
├─ Access: Outbound HTTPS requests
├─ Data Sent:
│  • Photos (base64 encoded)
│  • Text descriptions
│  • Geographic coordinates
│  • NO PII (usernames, passwords, emails)
├─ Threats:
│  • API key exposure (hardcoded in client)
│  • Data leakage to third party
│  • Service unavailability
│  • Rate limiting/quota exhaustion
└─ Mitigations:
   ✅ API key in server-side env var only
   ✅ No API key in frontend bundle
   ✅ Sanitized data before sending
   ✅ Error handling (fallback to manual categorization)
   ✅ Request timeout (30s)

EE6: PostgreSQL Database
├─ Trust Level: 🟢 Internal (Trusted Infrastructure)
├─ Access: Backend server only (connection pool)
├─ Credentials: DATABASE_URL env var
├─ Threats:
│  • SQL injection
│  • Database credential leak
│  • Unauthorized direct access
│  • Data exfiltration
└─ Mitigations:
   ✅ Parameterized queries (pg library)
   ✅ Connection pooling (max 20)
   ✅ Database user with limited privileges
   ✅ Firewall rules (localhost only in dev)
   ✅ Encrypted connections (TLS in prod)
   ⚠️  TODO: Database backup encryption
```

---

## Critical Data Flows

**High-risk data flows requiring special attention**

### Flow 1: Authentication Credential Flow

```
┌─────────────┐                          ┌──────────────┐
│   Client    │                          │   Backend    │
│  (Browser)  │                          │   (Express)  │
└──────┬──────┘                          └──────┬───────┘
       │                                        │
       │ (1) POST /api/auth/login               │
       │     { username, password } ⚠️ PLAINTEXT│
       │────────────────────────────────────────►
       │                                        │
       │                          (2) Rate limit check
       │                          (3) Validate input
       │                          (4) Query user by username
       │                          (5) Compare password:
       │                              bcrypt.compare(
       │                                password,
       │                                stored_hash
       │                              )
       │                          (6) Generate JWT token
       │                                        │
       │◄────────────────────────────────────────
       │ (7) { token: "eyJhbGc..." }            │
       │                                        │
       │ (8) Store in localStorage ⚠️ XSS RISK  │
       │                                        │

⚠️  THREATS:
  • Plaintext password in transit (TLS required)
  • Password in browser memory (XSS can steal)
  • Token in localStorage (XSS can steal)
  • Brute force attacks on login endpoint
  • Man-in-the-middle attacks (if not HTTPS)

✅ MITIGATIONS:
  • Enforce HTTPS/TLS in production
  • Rate limiting (5 attempts/15min)
  • bcrypt slow hashing (prevents rainbow tables)
  • JWT with short expiry (7 days, configurable)
  • Content Security Policy (helmet)
  • Future: httpOnly cookies instead of localStorage
```

### Flow 2: File Upload Flow

```
┌─────────────┐                          ┌──────────────┐
│   Client    │                          │   Backend    │
└──────┬──────┘                          └──────┬───────┘
       │                                        │
       │ (1) POST /api/media/upload             │
       │     Authorization: Bearer <JWT>        │
       │     Body: { photo: "data:image/jpeg;   │
       │             base64,/9j/4AAQSkZJRg..." }│
       │     ⚠️ 50MB LIMIT                       │
       │────────────────────────────────────────►
       │                                        │
       │                          (2) Verify JWT
       │                          (3) Check file size
       │                          (4) Validate MIME type
       │                          (5) Decode base64
       │                          (6) Generate UUID filename
       │                          (7) Save to /uploads/
       │                          (8) Return URL
       │                                        │
       │◄────────────────────────────────────────
       │ (9) { url: "/uploads/<uuid>.jpg" }     │
       │                                        │

⚠️  THREATS:
  • Malicious file uploads (malware, scripts)
  • Path traversal (../../etc/passwd)
  • File type spoofing (rename .exe to .jpg)
  • Storage exhaustion (upload flooding)
  • XXE attacks (XML-based image formats)

✅ MITIGATIONS:
  • Auth required (JWT validation)
  • File size limit (50MB max)
  • MIME type validation (image/video only)
  • UUID-based filenames (no user input)
  • Multer library (secure file handling)
  • Future: Virus scanning, magic number validation
```

### Flow 3: Geolocation Data Flow

```
┌─────────────┐                          ┌──────────────┐
│   Client    │                          │   Backend    │
│  (Browser)  │                          │   (Express)  │
└──────┬──────┘                          └──────┬───────┘
       │                                        │
       │ (1) navigator.geolocation.             │
       │     getCurrentPosition()               │
       │     ⚠️ USER PERMISSION REQUIRED        │
       │                                        │
       │ (2) { lat: 33.8886, lng: 35.4955 }     │
       │     ⚠️ PRECISE LOCATION DATA            │
       │                                        │
       │ (3) POST /api/reports                  │
       │     { lat, lng, ... }                  │
       │────────────────────────────────────────►
       │                                        │
       │                          (4) Validate coordinates
       │                          (5) Convert to PostGIS:
       │                              ST_SetSRID(
       │                                ST_MakePoint(lng, lat),
       │                                4326
       │                              )
       │                          (6) Store in database
       │                          (7) Create spatial index
       │                                        │

⚠️  THREATS:
  • User tracking (precise location logging)
  • Location spoofing (fake coordinates)
  • Re-identification (combining location + time)
  • Data aggregation attacks

✅ MITIGATIONS:
  • User consent required (browser prompt)
  • Location data for civic purpose only
  • No location history per user
  • Spatial indexing (performance, not privacy)
  • Future: Location fuzzing (±100m radius)
```

### Flow 4: External AI API Flow

```
┌─────────────┐         ┌──────────────┐         ┌─────────────┐
│   Client    │         │   Backend    │         │   Gemini    │
└──────┬──────┘         └──────┬───────┘         │     API     │
       │                       │                 └──────┬──────┘
       │                       │                        │
       │ (1) Submit report     │                        │
       │────────────────────────►                       │
       │                       │                        │
       │                       │ (2) POST /v1/models/   │
       │                       │     gemini-2.5-flash   │
       │                       │     Headers:           │
       │                       │       x-goog-api-key   │
       │                       │     Body: {            │
       │                       │       contents: [{     │
       │                       │         parts: [{      │
       │                       │           text: "...", │
       │                       │           inline_data: │
       │                       │             "base64..."│
       │                       │         }]             │
       │                       │       }]               │
       │                       │     }                  │
       │                       │─────────────────────────►
       │                       │                        │
       │                       │◄─────────────────────────
       │                       │ (3) { category, severity }
       │                       │                        │
       │◄───────────────────────                        │
       │ (4) Report created                             │
       │                                                │

⚠️  THREATS:
  • API key exposure (hardcoded in client)
  • Sensitive data leakage to Google
  • Service unavailability (dependency)
  • Cost exhaustion (API quota)
  • Data retention by third party

✅ MITIGATIONS:
  • API key server-side only (env var)
  • No PII sent (usernames, emails excluded)
  • Timeout handling (30s max)
  • Fallback to manual categorization
  • Error logging (not user data)
  • Request sanitization
```

---

## Summary: High-Risk Areas

| Risk Level | Component | Threat | Current Mitigation | Priority |
|------------|-----------|--------|-------------------|----------|
| 🔴 **CRITICAL** | User Credentials | Password theft, brute force | bcrypt + salt, rate limiting | ✅ Implemented |
| 🔴 **CRITICAL** | JWT Tokens | Token theft (XSS) | localStorage (XSS vulnerable) | ⚠️ TODO: httpOnly cookies |
| 🔴 **CRITICAL** | API Keys | Exposure in client | Server-side env vars | ✅ Implemented |
| 🟡 **HIGH** | File Uploads | Malware, path traversal | Type validation, UUID names | ⚠️ TODO: Virus scan |
| 🟡 **HIGH** | SQL Injection | Database compromise | Parameterized queries | ✅ Implemented |
| 🟡 **HIGH** | Location Data | User tracking | User consent, no history | ⚠️ TODO: Fuzzing |
| 🟢 **MEDIUM** | XSS Attacks | Code injection | Input validation, CSP | ✅ Implemented |
| 🟢 **MEDIUM** | CSRF Attacks | Unauthorized actions | CORS whitelist | ✅ Implemented |
| 🟢 **MEDIUM** | Rate Limiting | DoS attacks | 5 req/15min on auth | ✅ Implemented |

---

**Next Steps for Threat Modeling:**
1. Review UML diagrams (see THREAT_MODEL_UML.md)
2. Conduct STRIDE analysis per component
3. Prioritize remaining mitigations (httpOnly cookies, virus scan, MFA)
4. Perform penetration testing
5. Document security incident response plan

---

**Document Version:** 1.0  
**Last Updated:** November 16, 2025  
**Reviewed By:** Security Team (pending)
